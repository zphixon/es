#!/usr/bin/env python
#env = Environment()
#env.Replace(CC = "clang")
#target = env.Program(target="build/es", source=["es.c"])

SConscript("main.scons", variant_dir="build", duplicate=0)

#Default(target)
