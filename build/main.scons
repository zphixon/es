#!/usr/bin/env python

env = Environment()
env.Append(CCFLAGS = ['-g']) #, '-fsanitize=address', '-fno-omit-frame-pointer'])
env.Replace(CC = "clang")
files = Glob("source/*.c")
#libs = ["lua", "ncurses"]
env.Program("es", files)#, LIBS = libs)

