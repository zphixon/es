#!/usr/bin/env python

env = Environment()
env.Append(CCFLAGS = ['-g'])
env.Replace(CC = "clang")
files = Glob("source/*.c")
libs = ["lua50", "ncurses"]
env.Program("es", files, LIBS = libs)

